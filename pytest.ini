[pytest]
; Colección acotada a tests/: pipelines/mr/test_path_gamma_mr.py es un script
; standalone del estudio ("no pytest", se corre con python directamente) y los
; sandboxes no contienen tests.
testpaths = tests
